import { StyleSheet } from "react-native";

export default StyleSheet.create({
  main: {
    alignItems: "center",
    justifyContent: "center",
  },
  image: {
    position: "absolute",
    resizeMode: "cover",
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  },
});
